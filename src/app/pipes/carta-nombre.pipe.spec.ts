import { CartaNombrePipe } from './carta-nombre.pipe';

describe('CartaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new CartaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
